{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "techo",
  "main": "src/index.ts",
  "compatibility_date": "2026-09-01",
  "assets": {
    "directory": "./public",
    "binding": "ASSETS",
    "not_found_handling": "404-page",
    // only API and photos hit the Worker first; pages & scripts are served straight from the edge
    "run_worker_first": ["/api/*", "/img/*"]
  },
  "d1_databases": [
    { "binding": "DB", "database_name": "techo-db", "database_id": "09d467c6-98de-46e4-8eb3-db07c06c6f45" }
  ],
  "r2_buckets": [
    { "binding": "PHOTOS", "bucket_name": "techo-photos" }
  ],
  // admin password: `npx wrangler secret put ADMIN_PASSWORD` (not in this file)
  "ratelimits": [
    { "name": "LOGIN_LIMITER", "namespace_id": "1001", "simple": { "limit": 5, "period": 60 } }
  ],
  "vars": {
    // the journal's days follow this zone (nightly draft, "today")
    "TIMEZONE": "Asia/Shanghai"
  },
  // 23:30 Asia/Shanghai: write today's draft from the jots if the desktop task (22:00) didn't
  "triggers": { "crons": ["30 15 * * *"] },
  "observability": { "enabled": true }
}
